import React from 'react';
import logo from '../img/acoustic.png';

const Header = () => {
    return (
        <header className='text-center'>
            <img src={ logo } alt='Accoustic logo'/>
        </header>
    );
};

export default Header;
